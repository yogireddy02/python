DB_HOST=<replace it with database host>
DB_PORT=<replace it with database port>
DB_NAME=<replace it with db name>
DB_USER=<replace it with db user>
DB_PASSWORD=<replace it with db password>
OPENAI_API_KEY=<replace it with  open ai key>